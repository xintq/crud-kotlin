app.name=default_spring_boot
app.description=${app.name} is a Spring Boot application

com.example.title=CRUD
com.example.description=A Smart CRUD Sys

spring.datasource.url = jdbc:mysql://localhost:3306/crud?useUnicode=true&characterEncoding=UTF-8
#spring.datasource.url = jdbc:h2:file:/Volumes/MacHDD/code/kotlin/crud/test.db;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username = root
spring.datasource.password = 123456
spring.datasource.driverClassName = com.mysql.jdbc.Driver
#spring.datasource.driverClassName = org.h2.Driver

# Specify the DBMS
spring.jpa.database = MYSQL

#logging.level.org.hibernate=DEBUG

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

spring.profiles.active=dev
